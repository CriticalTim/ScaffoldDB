@inherits LayoutComponentBase

<div class="page">
    @*<div class="sidebar">
        <NavMenu />
    </div>*@

    <main>
        <div class="top-row px-4">
            <div class="col-auto">
                <h3>Database Manipulator</h3>
            </div>
            <div class="col-8">
            </div>
            <div class="col-auto">
                <a class="btn btn-primary" href="/">Start</a>
                @*<NavLink href="" class="nav-link btn btn-primary" Match="NavLinkMatch.All">Start</NavLink>*@
            </div>

        </div>@*https://learn.microsoft.com/aspnet/core/*@

            <article class="content px-4">
                @Body
            </article>
    </main>
</div>

<div id="blazor-error-ui">
    An unhandled error has occurred.
    <a href="" class="reload">Reload</a>
    <a class="dismiss">🗙</a>
</div>

<style>
    /* Sidebar Styling */
    page{
        background-image: ;
    }

    .sidebar {
        background: #1e2a39;
        color: #fff;
        width: 250px;
        height: 100vh;
        padding: 20px 15px;
        position: fixed;
        box-shadow: 0 4px 15px rgba(0, 0, 0, 0.3);
        border-radius: 10px;
        font-family: 'Segoe UI', Tahoma, Geneva, Verdana, sans-serif;
        display: flex;
        flex-direction: column;
        justify-content: space-between;
    }

    /* Sidebar Menu Styling */
    .sidebar-menu {
        list-style: none;
        padding: 0;
        margin: 0;
    }

        .sidebar-menu li {
            margin-bottom: 15px;
        }

        .sidebar-menu a {
            display: block;
            color: #fff;
            text-decoration: none;
            padding: 10px 15px;
            border-radius: 5px;
            transition: all 0.3s ease;
            font-size: 1rem;
        }

            .sidebar-menu a:hover {
                background: #00d8ff;
                color: #000;
                box-shadow: 0 2px 10px rgba(0, 216, 255, 0.6);
            }

            /* Active Link Style */
            .sidebar-menu a.active {
                background: linear-gradient(135deg, #66ffcc, #33ccff);
                color: #000;
                box-shadow: 0 4px 15px rgba(51, 204, 255, 0.6);
            }

        /* Logout Section */
        .sidebar-menu li:last-child a {
            background: linear-gradient(135deg, #ff4d4d, #ff6666);
            color: #fff;
            box-shadow: 0 2px 10px rgba(255, 77, 77, 0.5);
            font-weight: bold;
        }

            .sidebar-menu li:last-child a:hover {
                background: #ff3333;
                box-shadow: 0 4px 15px rgba(255, 51, 51, 0.8);
            }
</style>